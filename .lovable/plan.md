
Всё, что описано в задаче, уже реализовано в предыдущей итерации и находится в кодовой базе:

## Уже сделано

### `src/data/applications.ts`
- В `Application` добавлены поля `consentRaw`, `contractRaw` (сохраняют исходные значения из API).
- Экспортированы хелперы `hasBudgetConsent(app)` и `hasPaidContract(app)` с нормализацией (`trim` + `toLowerCase` + свёртка пробелов): согласие = `электронное`/`бумажное`, договор = `да`.

### `src/pages/Index.tsx`
- В сетке KPI карточка «Нужны данные» заменена на `ConfirmationsKpiCard` с заголовком «Подтверждения Елисея» и двумя строками `Согласий: X` / `Договоров: Y`.
- Карточка — это `<a href="/confirmations" target="_blank" rel="noopener noreferrer">`, открывается в новой вкладке.
- Старая метрика `missingData` вынесена в отдельный спокойный блок «Полнота данных» (`bg-secondary/40 border-dashed`) внизу главной, перед футером.

### `src/pages/Confirmations.tsx` (новая страница)
- Загружает `getDashboardData()`, считает `budgetConsents` и `paidContracts` через хелперы.
- Две визуально разделённые секции с заголовками «Поданные согласия · Бюджет» и «Заключённые договоры · Платное», разный акцент рамки (`border-l-success` / `border-l-accent`) и разные бейджи.
- Пустые секции не рендерятся; если оба списка пусты — сообщение «Пока нет ни согласий, ни договоров».
- В карточке направления: вуз, группа, приоритет, балл, общая позиция, тип подтверждения, дата снимка, ссылка «История» на `/dynamics?groupId=…`.

### `src/App.tsx`
- Добавлен маршрут `<Route path="/confirmations" element={<Confirmations />} />`.

## Что делать сейчас

Дополнительных изменений не требуется — все пункты issue #12 закрыты. Если вы видите расхождение с ожиданиями (например, другие допустимые значения `consent`/`contract`, другой визуальный акцент разделения, доп. поля в карточке подтверждения), уточните — тогда я перепланирую точечную правку.
